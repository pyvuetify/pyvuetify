import pyvuetify as v

v.Html(
    tag="div",
    class_="d-flex flex-column ga-4 align-center py-3",
    children=[
        v.AvatarGroup(
            # JS expression, needs manual conversion
            key="size",
            # JS expression, needs manual conversion
            gap="gap",
            # JS expression, needs manual conversion
            items="items",
            # JS expression, needs manual conversion
            size="size",
        ),
    ],
)
