import pyvuetify as v

v.Select(
    # JS expression, needs manual conversion
    items="items",
    # JS expression, needs manual conversion
    menu_props="{ scrim: true, scrollStrategy: 'close' }",
    label="Label",
)
