import pathlib

import anywidget
import anywidget._traits as _awt
import traitlets

bundled_path = pathlib.Path(__file__).parents[1] / "_static"


def _children_to_json(value, obj):
    """Serialize children: widget → 'anywidget:<id>', str → str, list → list."""
    if value is None:
        return None
    if isinstance(value, list):
        return [_children_to_json(item, obj) for item in value]
    return _awt._widget_to_json(value, obj)


def _children_from_json(value, obj):
    """Pass-through for incoming children state."""
    return value


class {{ v_component.name.removeprefix("V") }}(anywidget.AnyWidget):
    """The {{ v_component.name.removeprefix("V") }} component.

    {{ v_component.description | md_to_rst }}

    Args:
        children: The child nodes of the DOM element.
        style: Custom CSS styles to apply to the component.
        klass: Custom CSS classes to apply to the component.
        {%- for p in v_component.attributes %}
        {{ p.name | to_snake }}: {{ p.description | default("No description available.") | md_to_rst }}
        {%- endfor %}

    Slots:
        {%- for s in v_component.slots %}
        {%- if (s.name | to_snake).isidentifier() %}
        slot_{{ s.name | to_snake }}: {{ s.description | default("No description available.") | md_to_rst }}
        {%- endif %}
        {%- endfor %}

    Events:
        {%- for e in v_component.events %}
        {{ e.name }}: {{ e.description | default("No description available.") | md_to_rst }}
        {%- endfor %}

    Returns:
        A {{ v_component.name }} widget.

    seealso:
        `{{ v_component["doc-url"] }}`__
    """

    _esm = (bundled_path / "{{ v_component.name }}.js").read_text()
    _css = (bundled_path / "pyvuetify.css").read_text()

    children = traitlets.Any(allow_none=True).tag(sync=True, to_json=_children_to_json, from_json=_children_from_json)
    """The child nodes of the DOM element."""

    style = traitlets.Unicode(allow_none=True).tag(sync=True)
    """Custom CSS styles to apply to the component."""

    klass = traitlets.Unicode(allow_none=True).tag(sync=True)
    """Custom CSS classes to apply to the component."""

{%- for p in v_component.attributes %}

    {{ p.name | to_snake }} = traitlets.{{ p.value.type | map_type }}(allow_none=True).tag(sync=True)
    """{{ p.description | default("No description available.") | md_to_rst }}"""

{%- endfor %}

{%- for s in v_component.slots %}
{%- if (s.name | to_snake).isidentifier() %}

    slot_{{ s.name | to_snake }} = traitlets.Any(allow_none=True).tag(sync=True)
    """{{ s.description | default("No description available.") | md_to_rst }}"""

{%- endif %}
{%- endfor %}

    def __init__(
        self,
        children=None,
        style=None,
        klass=None,
        {%- for p in v_component.attributes %}
        {{ p.name | to_snake }}={{ p.get("default", "null").replace("\n", "") | map_default }},
        {%- endfor %}
        {%- for s in v_component.slots %}
        {%- if (s.name | to_snake).isidentifier() %}
        slot_{{ s.name | to_snake }}=None,
        {%- endif %}
        {%- endfor %}
        **kwargs
    ):
        """Initialize a ``{{ v_component.name.removeprefix("V") }}`` widget."""
        self.children = children
        self.style = style
        self.klass = klass
        {%- for p in v_component.attributes %}
        self.{{ p.name | to_snake }} = {{ p.name | to_snake }}
        {%- endfor %}
        {%- for s in v_component.slots %}
        {%- if (s.name | to_snake).isidentifier() %}
        self.slot_{{ s.name | to_snake }} = slot_{{ s.name | to_snake }}
        {%- endif %}
        {%- endfor %}

        super().__init__(**kwargs)